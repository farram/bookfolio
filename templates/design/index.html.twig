{% extends 'design.html.twig' %}

{% block title %}
	{{design.title}}
	-
	{{"Prévisualisation" | trans}}
{% endblock %}

{% block body %}
	<div id="preview-bar" class="pt-3 pb-3">

		<div class="container-fluid">
			<div class="row">
				<div class="col-lg-2 text-start align-self-center">
					<div class="pl-5">
						<a href="{{ path('home') }}"><img src="{{ asset('assets/img/logo-color.png') }}" class="img-responsive" width="100" alt="Bookfolio"></a>
					</div>
				</div>
				<div class="col-lg-8 text-center">
					<div class="row">
						<div class="col-lg-3 align-self-center">
							<a class="btn btn-outline-primary" href="#" role="button">{{"Retour aux designs" | trans}}</a>
						</div>
						<div class="col-lg-6 align-self-center">
							<h1>{{design.title}}</h1>
						</div>

						<div class="col-lg-3 align-self-center">
							<ul class="p-0 m-0">
								<li class="icon-row">
									<a id="viewport-change-desktop" href="#">
										<i class="fas fa-desktop"></i>
									</a>
								</li>
								<li class="icon-row">
									<a id="viewport-change-laptop" href="#">
										<i class="fas fa-laptop"></i>
									</a>
								</li>
								<li class="icon-row">
									<a id="viewport-change-tablet" href="#">
										<i class="fas fa-tablet"></i>
									</a>
								</li>
								<li class="icon-row">
									<a id="viewport-change-mobile" href="#">
										<i class="fas fa-mobile"></i>
									</a>
								</li>
							</ul>
						</div>


					</div>
				</div>
				<div class="col-lg-2 text-right align-self-center">
					<a class="btn btn-primary d-inline-block" href="{{ path('manage_book_design_choice',{'id': design.id}) }}" role="button">{{"Choisir ce design" | trans}}</a>
				</div>
			</div>
		</div>
	</div>
	<div class="box_layout pl-5 pr-5">
		<iframe src="{{path('design',{'slug': slug})}}" align="middle" frameborder="0"></iframe>
	</div>

{% endblock %}
